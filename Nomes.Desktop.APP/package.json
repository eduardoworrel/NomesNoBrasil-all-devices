{
  "name": "app",
  "version": "1.0.0",
  "description": "app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run compile && electron",
    "compile": "tsc"
  },
  "author": "Eduardo Worrel",
  "license": "ISC",
  "dependencies": {
    "electron": "^16.0.7",
    "typescript": "^4.5.5"
  }
}
